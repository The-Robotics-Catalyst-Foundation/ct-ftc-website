<script lang="ts">
	// This route only exists to host the `updateProfile` form action used by
	// the settings modal in the admin layout - the load function always
	// redirects away, so this markup never actually renders.
</script>
